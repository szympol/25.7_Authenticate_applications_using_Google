html
    head
        title Login to space
        style
            include login.css
    body
        .description Are you a dreamer? If yes, you've come to the right place. Please login in to start travelling the Unknown. 
        .login-page
            .form
                form.login-form(action='/auth/google', method='GET')
                    input(type='text', placeholder='username')
                    input(type='password', placeholder='password')
                    button login